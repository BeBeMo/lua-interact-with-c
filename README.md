# lua interact with c++

###### compile command:
	g++ -o test test.cpp -I slua/include/  -L slua/lib/ slua/lib/linux/liblua.a
###### run command :
	./test
###### the output result :
```
str = this is lua
tbl:name = mao
Result is 30
```
